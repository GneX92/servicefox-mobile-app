import { View } from 'react-native';

// The actual logout logic lives in the tab layout's confirmation modal.
// This placeholder screen keeps the tab route valid without triggering sign-out automatically.
export default function LogoutScreen() {
  return <View />;
}
